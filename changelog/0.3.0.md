# 0.3.0 — 2026-08-28

Terminal panel + explorer context menu + plugin rail.

See `CHANGELOG.md:22` for the full entry.

- **Terminal panel** — `src/shell/TerminalPanel.tsx:1` xterm.js (`@xterm/xterm@^6.0.0` + `@xterm/addon-fit@^0.11.0`) with `TerminalPanel.css:1`, cwd synced to explorer selection (`useTerminalCwd` → selected/children/targetCwd/active doc/root), floating draggable panel + Document PiP / Tauri `WebviewWindow` / `window.open` pop-out, `src/store/terminal.ts:1` Zustand `useTerminal` (`isOpen/targetCwd/openAt/toggle`) + `openTerminalAt()` imperative helper, `src/App.tsx:540` standalone `TerminalStandalone` / `isTerminalWindow()` for out-of-window hosting, `src/shell/PluginRail.tsx:1` 44px hairline rail left-of-explorer (`PluginRail.css:1`) with extensible `PLUGINS` registry.

- **Explorer context menu** — `src/shell/ExplorerContextMenu.tsx:1` bubble/right-click menu (new file/folder, open in terminal, reveal in OS, cut/copy/paste, rename, refactor, delete) + rename/delete confirm dialogs, `src/ui/ContextMenu.tsx:1` + `ContextMenu.css:1` Radix `ContextMenu` primitive, `src/ui/Icon.tsx:68` new icons, `src/ui/Dropdown.tsx`/`Popover.tsx` polish, `src/shell/SideBar.tsx:1` wires per-row `ExplorerContextMenu` + `openTerminalAt(cwd)` routing, `src/store/explorer.ts:56` adds `clipboard: ClipboardEntry|null` + actions `renamePath/moveTo/deletePath/copyTo/setClipboard/pasteInto/openInTerminal/revealInOS/refactor` with eager cache remap + `nextAvailableDest` (`name copy` / `name copy (N)`) collision handling, `src/store/explorer.test.ts:1` 5 new tests (createFile+rename, createFolder+delete, copy paste, cut paste, rename validation).

- **Host + bridge** — `src-tauri/src/lib.rs:191` new commands `rename/delete/copy/open_in_terminal/reveal_in_folder/open_with_os` (`copy_dir_recursive` helper, `open_in_terminal` macOS `open -a Terminal` / Windows `cmd /C start` / Linux `x-terminal-emulator|gnome-terminal|konsole|alacritty|xterm` probe, `reveal_in_folder` Finder/Explorer/xdg-open), registered in `invoke_handler`, `src-tauri/capabilities/default.json:5` adds `main+terminal` windows + `core:window:allow-create|set-focus|show|hide|webview:allow-create*` caps, `src/bridge/commands.ts:12` adds `copyPath/openInTerminal/revealInOS/openWithOS` wrappers + IPC fallback (`Load failed/custom protocol/callback id` → mock) + memory mocks for `rename/delete/copy`, `src/App.css:1` `app__rail` layout.

- **Version bump** — `0.2.2 → 0.3.0` across `package.json`/`Cargo.toml`/`tauri.conf.json`/`Cargo.lock`.
