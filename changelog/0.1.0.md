# 0.1.0 — 2026-08-27

Initial scaffolding release.

See `CHANGELOG.md:18` for the full entry. This per-version file mirrors that heading for tooling that expects one file per release (e.g., release notes generators).

- App shell, document store, command registry, bridge with browser mock.
- IR (`version: 1`), Markdown/Code/Rich surfaces.
- Theming, motion, UI primitives, Tauri host commands.
- Docs (`README`, `explanation`, `description` 300-char, `worklog`, `designlabs`, `docs/` Diátaxis).

Known limitations: file watcher not wired, Rich menus staged, session restore best-effort, single-window/local-files only.
