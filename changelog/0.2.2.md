# 0.2.2 — 2026-08-28

Amber theme to test OTA. Also fixes `Merge pull request #6` / `develop.yml` triggering on `main`.

See `CHANGELOG.md:10` for the full entry.

- **Amber theme** — `src/theme/ThemeProvider.tsx:12` adds `amber` to `ThemeId`/`ORDER`/`resolveTheme`, `src/theme/tokens.css:244` new `:root[data-theme="amber"]` warm light theme (bg #fffbeb, surfaces #fef3c7→#fcd34d, accent #d97706), `src/shell/TitleBar.tsx:23` adds `amber` to `THEME_OPTIONS` + 5-swatch grid, `src/shell/TitleBar.css:70` grid 3×2 + `.titlebar__swatch--amber` #f59e0b. Selectable via TitleBar theme menu, persisted via `LazyStore` + `localStorage`, cycles via `cycle()`.

- **Fix #6 / develop workflow** — `src-tauri/tauri.conf.json:71` pubkey already fixed to single-line in 0.2.1, `Cargo.lock` synced. `.github/workflows/develop.yml:27` `ci` now `if: github.ref == 'refs/heads/develop' || pull_request || workflow_dispatch` to prevent running on `main` push (was failing on main). `.github/workflows/release.yml:85` `verify-ota` now `uses: actions/checkout@v4` before `gh release view` (was `not a git repository` + `latest.json not found`).

- **Version bump** — `0.2.1 → 0.2.2` across `package.json`/`Cargo.toml`/`tauri.conf.json`.
